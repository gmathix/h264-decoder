In this folder are the templates of scalar functions that have vectorized variants in the x86_64 folder.
Other files have templates (such as inter_template.c) but nowhere they are replaced with SIMD variants,
hence they are not placed here.